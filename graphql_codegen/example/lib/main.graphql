query FetchPerson ($id: ID!) {
    fetch_person(id: $id) {
        ...PersonSummary
        parents {
            ...PersonSummary
        }
        children {
            ...PersonSummary
        }
    }
}